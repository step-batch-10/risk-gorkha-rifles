import { assertEquals } from "assert";
import { describe, it } from "testing";
import Server from "../src/server.ts";

describe("tests for app dynamic routes", () => {
  it("tests for login route", () => {
    const server = new Server();
  });
});
